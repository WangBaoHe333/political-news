# 开发依赖 (也可以通过 pip install -e ".[dev]" 安装)
# 此文件为向后兼容，推荐使用 pyproject.toml 管理依赖

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# 代码质量和格式化
black>=23.0.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.6.0

# 安全
bandit>=1.7.0
safety>=2.3.0

# 开发工具
pre-commit>=3.5.0

# 测试工具（可选）
factory-boy>=3.3.0
Faker>=18.0.0
freezegun>=1.2.0