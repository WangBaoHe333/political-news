# 时政系统环境变量示例

# 数据库
DATABASE_URL=sqlite:///./political_news.db

# 启动行为
# AUTO_SYNC_ON_STARTUP=1 会在每次服务启动时自动抓取最新内容（推荐开启）
AUTO_SYNC_ON_STARTUP=1
# BOOTSTRAP_RECENT_NEWS_ON_STARTUP=1 会在数据库缺少近两年数据时自动补齐
BOOTSTRAP_RECENT_NEWS_ON_STARTUP=1
# 正式环境建议设置，用于保护手动同步接口
SYNC_ADMIN_TOKEN=
SCHEDULED_SYNC_INTERVAL_HOURS=1
SCHEDULED_SYNC_TIMEZONE=Asia/Shanghai
EXPOSE_API_DOCS=1
HTTP_VERIFY_TLS=1

# 部署访问地址
CORS_ORIGINS=http://localhost:8000
